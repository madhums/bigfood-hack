<nav class="navbar navbar-expand-lg border-bottom mb-4">
  <div class="container py-2">
    <div class="d-flex align-items-center">
      <%= link_to t('application_name'), root_path, class: "navbar-brand" %>
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menu" aria-controls="menu" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="menu">
      <ul class="navbar-nav mr-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to t('admin'), admin_root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('jobs'), admin_mission_control_jobs_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('invite'), new_user_invitation_path, class: "nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle nav-account-dropdown" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">
              <%= t("account") %>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to t('profile'), edit_user_registration_path, class: "dropdown-item" %>
              </li>
              <li>
                <%= link_to t('logout'), destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t('login'), new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
